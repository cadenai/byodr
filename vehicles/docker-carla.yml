version: '2'
services:
  inference:
    build:
      context: ./
      dockerfile: inference/Dockerfile.x86
  vehicle:
    build:
      context: ./
      dockerfile: vehicles/carla09/Dockerfile
    image: centipede2donald/byodr-ce:carla096
    volumes:
      - volume_byodr:/byodr
      - ./common:/common
      - ./vehicles/carla09:/app
      - ${DC_CONFIG_DIR}:/config
